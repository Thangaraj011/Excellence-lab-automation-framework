# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: seed.spec.js >> Employee Homepage Validation
- Location: tests/specs/seed.spec.js:4:7

# Error details

```
Error: Failed to load test data from "/home/runner/work/playwright-pom-framework/playwright-pom-framework/tests/data/Employee Homepage Validation.json": ENOENT: no such file or directory, open '/home/runner/work/playwright-pom-framework/playwright-pom-framework/tests/data/Employee Homepage Validation.json'
```

# Test source

```ts
  1   | import users from '../data/users.json';
  2   | import crypto from 'crypto';
  3   | const fs = require("fs");
  4   | const path = require("path");
  5   | 
  6   | const SECRET = "j4dFtlJH68pdc9A4";
  7   | const FRONTEND_BASE_URL = process.env.BASE_FE_URL || 'http://localhost:3000';
  8   | 
  9   | export class GenericUtils {
  10  |     constructor(context, testInfo) {
  11  |         this.context = context;
  12  |         this.testInfo = testInfo;
  13  |         this.jsonObject = null;
  14  |         this.map = null;
  15  |     }
  16  | 
  17  |     async base64Url(buf) {
  18  |         return Buffer.from(buf)
  19  |             .toString("base64")
  20  |             .replace(/\+/g, "-")
  21  |             .replace(/\//g, "_")
  22  |             .replace(/=/g, "");
  23  |     }
  24  | 
  25  |     async generateJWT(userRole) {
  26  |         const role = userRole;
  27  |         if (!role) throw new Error('USER_ROLE is not defined in .env');
  28  |         const user = users[role];
  29  |         if (!user) {
  30  |             throw new Error(`Invalid user role provided: ${role}. Please check users.json keys.`);
  31  |         }
  32  |         const payload = { ...user, exp: Math.floor(Date.now() / 1000) + 86400 * 7 };
  33  |         const header  = await this.base64Url(Buffer.from(JSON.stringify({ alg: "HS256", typ: "JWT" })));
  34  |         const body    = await this.base64Url(Buffer.from(JSON.stringify(payload)));
  35  |         const signingInput = `${header}.${body}`;
  36  |     
  37  |         const sig = crypto
  38  |             .createHmac("sha256", SECRET)
  39  |             .update(signingInput)
  40  |             .digest();
  41  | 
  42  |         return `${signingInput}.${await this.base64Url(sig)}`;
  43  |     }
  44  | 
  45  |     async setupAuthCookie(token) {
  46  |         const context = this.context;
  47  |         if (!context) {
  48  |             throw new Error('Browser context is not initialized. Call init() before setupAuthCookie().');
  49  |         }
  50  | 
  51  |         await context.addCookies([
  52  |         {
  53  |             name:     "token",
  54  |             value:    token,
  55  |             domain:   "learning-buddy-dev-backend-service-346514237201.us-central1.run.app",
  56  |             path:     "/",
  57  |             httpOnly: false,
  58  |             secure:   true,
  59  |             sameSite: "None",
  60  |             expires:  Math.floor(Date.now() / 1000) + 604800, // 7 days
  61  |         },
  62  |         ]);
  63  |     }
  64  | 
  65  |     async setJsonData() {
  66  |         const fileName = `${this.testInfo.title}.json`;
  67  |         const filePath = path.resolve(__dirname, "../data", fileName);
  68  |         console.log(`filePath: ${filePath}`);
  69  |         try {
  70  |             const raw = fs.readFileSync(filePath, "utf-8");
  71  |             this.jsonObject = JSON.parse(raw);
  72  |             return this.jsonObject;
  73  |         } catch (err) {
> 74  |             throw new Error(`Failed to load test data from "${filePath}": ${err.message}`);
      |                   ^ Error: Failed to load test data from "/home/runner/work/playwright-pom-framework/playwright-pom-framework/tests/data/Employee Homepage Validation.json": ENOENT: no such file or directory, open '/home/runner/work/playwright-pom-framework/playwright-pom-framework/tests/data/Employee Homepage Validation.json'
  75  |         }
  76  |     }
  77  | 
  78  |     async jsonToMap() {
  79  |     if (!this.jsonObject) {
  80  |         await this.setJsonData();
  81  |     }
  82  |     this.map = new Map();
  83  |     const convert = (obj, currentMap) => {
  84  |         for (const [key, value] of Object.entries(obj)) {
  85  |             if (value !== null && typeof value === "object" && !Array.isArray(value)) {
  86  |                 // 1. Create a nested sub-map for structural integrity
  87  |                 const subMap = new Map();
  88  |                 currentMap.set(key, subMap);
  89  |                 if (currentMap !== this.map) {
  90  |                     this.map.set(key, subMap);
  91  |                 }
  92  |                 convert(value, subMap);
  93  |             } else {
  94  |                 currentMap.set(key, value);
  95  |                 this.map.set(key, value);
  96  |             }
  97  |         }
  98  |     };
  99  | 
  100 |     convert(this.jsonObject, this.map);
  101 |     return this.map;
  102 | }
  103 | 
  104 |     async getValueByKey(key) {
  105 |         if (!this.jsonObject) {
  106 |             await this.setJsonData();
  107 |         }
  108 |         const search = (obj) => {
  109 |             if (obj === null || typeof obj !== "object") return undefined;
  110 |             if (Object.prototype.hasOwnProperty.call(obj, key)) {
  111 |                 return obj[key];
  112 |              }
  113 |         for (const value of Object.values(obj)) {
  114 |             if (value !== null && typeof value === "object") {
  115 |                 const found = search(value);
  116 |                 if (found !== undefined) return found;
  117 |             }
  118 |         }
  119 |         return undefined;
  120 |         };
  121 |     return search(this.jsonObject);
  122 |     }
  123 | 
  124 | 
  125 | 
  126 | 
  127 | }
```