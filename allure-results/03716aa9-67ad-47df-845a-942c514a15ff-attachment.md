# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: token-generator.spec.js >> Generate and set token for local storage >> Generate token for Admin and save for later usage
- Location: tests/specs/token-generator.spec.js:30:5

# Error details

```
ReferenceError: page is not defined
```

# Test source

```ts
  1  | import {test, expect} from '../fixtures/index.js';
  2  | 
  3  | test.describe('Generate and set token for local storage', ()=>{
  4  | 
  5  | 
  6  |     test('Generate token for employee and save for later usage', async({plainPage, genericUtils})=>{
  7  |         const token = await genericUtils.generateJWT(process.env.EMP_USER);
  8  |         await genericUtils.setupAuthCookie(token);
  9  |         await page.reload();
  10 |         await plainPage.waitForLoadState('domcontentloaded');
  11 |         await plainPage.context().storageState({ path: 'auth/user.json' });
  12 |     });
  13 | 
  14 |     test('Generate token for Manager Indirect and save for later usage', async({plainPage, genericUtils})=>{
  15 |         const token = await genericUtils.generateJWT(process.env.MANAGER_IND_USER);
  16 |         await genericUtils.setupAuthCookie(token);
  17 |         await page.reload();
  18 |         await plainPage.waitForLoadState('domcontentloaded');
  19 |         await plainPage.context().storageState({ path: 'auth/managerindirect.json' });
  20 |     });
  21 | 
  22 |     test('Generate token for Manager Direct and save for later usage', async({plainPage, genericUtils})=>{
  23 |         const token = await genericUtils.generateJWT(process.env.MANAGER_DIR_USER);
  24 |         await genericUtils.setupAuthCookie(token);
  25 |         await page.reload();
  26 |         await plainPage.waitForLoadState('domcontentloaded');
  27 |         await plainPage.context().storageState({ path: 'auth/managerdirect.json' });
  28 |     });
  29 | 
  30 |     test('Generate token for Admin and save for later usage', async({plainPage, genericUtils})=>{
  31 |         const token = await genericUtils.generateJWT(process.env.ADMIN_USER);
  32 |         await genericUtils.setupAuthCookie(token);
> 33 |         await page.reload();
     |         ^ ReferenceError: page is not defined
  34 |         await plainPage.waitForLoadState('domcontentloaded');
  35 |         await plainPage.context().storageState({ path: 'auth/admin.json' });
  36 |     });
  37 | 
  38 |     test('Generate token for Manager-Admin and save for later usage', async({plainPage, genericUtils})=>{
  39 |         const token = await genericUtils.generateJWT(process.env.ADMIN_MANAGER_USER);
  40 |         await genericUtils.setupAuthCookie(token);
  41 |         await page.reload();
  42 |         await plainPage.waitForLoadState('domcontentloaded');
  43 |         await plainPage.context().storageState({ path: 'auth/adminmanager.json' });
  44 |     });
  45 | 
  46 | });
```